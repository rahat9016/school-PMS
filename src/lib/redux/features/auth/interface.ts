export interface IInitialState {
    userInformation:{
        _id: string;
        email: string;
        role: string;
        fullName:string;
        activeStatus: boolean;
        contact: string
    },
}