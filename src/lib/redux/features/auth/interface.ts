export interface IInitialState {
    userInformation:{
        id: string;
        email: string;
        role: string;
        fullName:string;
        activeStatus: boolean;
        contact: string
    },
}